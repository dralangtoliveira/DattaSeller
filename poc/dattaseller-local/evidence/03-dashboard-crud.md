# Dashboard e CRUD local

Validação visual em `http://127.0.0.1:8765`: o painel carregou com o estado **banco conectado**. Em uma amostra local descartável, a visão geral mostrou 2 leads, 1 proposta e 1 follow-up. A API criou, atualizou e removeu os dois registros; o banco terminou vazio.

Não foi salvo PNG porque o runtime local de captura visual não possui navegador headless instalado. A inspeção visual foi realizada pelo navegador local controlado e os dados não contêm leads reais.
