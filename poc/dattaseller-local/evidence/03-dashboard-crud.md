# Dashboard e CRUD local

Validação visual em `http://127.0.0.1:8765`: o painel carregou com o estado **banco conectado**. Em uma amostra local descartável, a visão geral mostrou 2 leads, 1 proposta e 1 follow-up. A captura está em `03-dashboard-crud.png`. A API criou, atualizou e removeu os dois registros; o banco terminou vazio.
