# Checkout Datta VPS

Estado: desabilitado. `DATTA_VPS_CHECKOUT_URL`, nome da oferta e preço estão vazios em `.env.example` e `app/prospector-config.example.json`. O MVP não cria checkout, não recebe cartão e não confirma pagamento.
